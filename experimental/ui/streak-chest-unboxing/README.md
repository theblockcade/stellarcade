# Streak Chest Unboxing Modal

Experimental daily quest streak chest unboxing dialog with tier glow styling and claim flow.

## Props

See `types.ts` for `StreakChestUnboxingProps`.

## Tests

From repo root (`apps/web` dependencies):

```bash
npx vitest run experimental/ui/streak-chest-unboxing/StreakChestUnboxing.test.tsx
```
